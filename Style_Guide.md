# 디자인 스타일 가이드 제작 총괄 프로세스

프로젝트 디자인 스타일 가이드를 만들 때 따르는 전체 진행 순서와 공통 규칙을 정리한다. 특정 프로젝트에 종속되지 않고 다른 프로젝트 파일에도 동일하게 적용할 수 있도록 작성한다. 개별 단계의 세부 규칙은 각 단계별 md 파일에 있고, 이 문서는 그 상위(총괄) 문서다.

## 0. 작업 방식

- md 파일이나 Figma 페이지는 사용자 지시 없이 **임의로 수정·정리하지 않는다.**
- 수정이 필요하다고 판단되면 먼저 사용자에게 확인을 요청하고, 승인받은 뒤에 진행한다.
- 예상과 다른 파일/페이지 상태(누락된 노드, 다른 구조 등)를 발견하면 사용자에게 알리되, 사용자가 의도적으로 만들어둔 것일 수 있으니 담백하게 사실만 보고하고 과잉 반응하지 않는다.
- **Variables/Styles(색상·Spacing·Grid·Text Style 등 종류를 불문하고)를 바인딩할 때, 대상이 반복되는 컴포넌트 인스턴스 내부 요소라면 인스턴스 각각에 개별 바인딩하지 않는다.** 먼저 인스턴스의 원본(마스터) 컴포넌트를 `getMainComponentAsync()` 등으로 찾아 **마스터 쪽에 한 번만 바인딩**하고, 모든 인스턴스가 상속받도록 한다. 인스턴스 오버라이드 노드에 개별 바인딩하면 마스터가 바뀌어도 해당 인스턴스에는 반영되지 않고, 나중에 값이 바뀔 때마다 모든 인스턴스를 일일이 찾아 다시 바인딩해야 하므로 Variables/Styles를 쓰는 의미가 없어진다.
  - 이미 인스턴스마다 개별(로컬) 바인딩이 걸려있는 상태를 발견하면, 마스터에 바인딩한 뒤 각 인스턴스의 로컬 바인딩(오버라이드)은 해제해 마스터 상속으로 정리한다.
  - Variant(`COMPONENT_SET`)로 구성된 컴포넌트는 관련된 모든 variant에 동일하게 바인딩한다 — 하나만 하면 다른 상태의 인스턴스는 빠질 수 있다.
  - 이 규칙은 04_2Font_Name.md의 "마스터 우선 규칙"(Text Style)에서 처음 정립됐으며, Color/Spacing/Grid 등 다른 모든 Variable·Style 종류에도 동일하게 적용한다.
  - **"마스터"가 한 겹이 아닐 수 있다.** 오버라이드 id(`I...;...;<원본id>`)의 마지막 세그먼트로 찾은 노드가 `type: "INSTANCE"`라면, 그 노드는 진짜 루트 마스터가 아니라 상위 마스터 컴포넌트 내부에 중첩된 또 다른 인스턴스일 뿐이다. 이 경우 그 노드에서 `getMainComponentAsync()`를 한 번 더 호출해 진짜 루트 컴포넌트(그리고 그것이 속한 `COMPONENT_SET`의 모든 variant)까지 확인하고, 거기에도 동일하게 바인딩해야 한다. 중첩 인스턴스에만 바인딩하고 진짜 루트를 건너뛰면, 같은 루트 컴포넌트를 쓰는 다른 위치(이번 분석 대상 밖의 인스턴스 등)에는 반영되지 않는다. (Radius 단계에서 Button/Small·GNB/Menu·Image·Button/Large 4건이 이렇게 누락됐다가 재점검으로 발견·수정됨 — `06_Radius.md` 참고)
- **Variables 이름의 영문 표기는 첫 글자를 대문자로 쓴다.** 예: `card` → `Card`, `tag` → `Tag`. 슬래시로 구분되는 각 구간(그룹명·값명 등)에 모두 적용한다. Color/Spacing/Radius 등 기존 Foundations 토큰은 이미 이 규칙을 따르고 있었고, Radius 단계에서 만든 Semantic/Component 토큰(`card`/`tag`)만 소문자로 등록됐다가 사용자 지시로 `Card`/`Tag`로 수정됨(`06_Radius.md` 참고). 앞으로 새 Variables를 등록할 때는 처음부터 대문자로 시작한다.

## 1. 전체 프로세스

스타일 가이드는 아래 순서로 진행한다. 각 단계는 프로젝트를 진행하면서 하나씩 다루고(오늘의 Font처럼), 완료된 단계마다 규칙을 md로 기록한다.

| 순서 | 단계 | 상태 | 비고 |
|---|---|---|---|
| 1 | FileName | **완료** | 아래 "3. 완료된 단계별 문서" 참고 |
| 2 | Layout | **완료** | 아래 "3. 완료된 단계별 문서" 참고 |
| 3 | Color | **완료** | 아래 "3. 완료된 단계별 문서" 참고 |
| 4 | Font | **완료** | 아래 "3. 완료된 단계별 문서" 참고 |
| 5 | Spacing | **완료** | 아래 "3. 완료된 단계별 문서" 참고. Figma 원본 가이드 페이지 자체가 "05 Spacing"으로 번호가 매겨져 있어 그대로 따름 |
| 6 | Radius | **완료** | 아래 "3. 완료된 단계별 문서" 참고 |
| 7 | Icon | **진행 중** | 아래 "3. 완료된 단계별 문서" 참고. "01_Size"(Keyline 크기 차트)부터 시작, 나머지 하위 섹션은 미정 |

> 아직 규칙이 정의되지 않은 단계는 임의로 규칙을 만들지 않고, 실제로 해당 단계를 진행할 때 사용자로부터 규칙을 학습해 문서화한다. 단계 번호는 진행 순서를 강제하지 않는다 — Radius·Spacing이 다른 단계보다 먼저/나중에 완료된 것처럼, 사용자가 원하는 순서대로 먼저 진행할 수 있다. 번호는 가급적 Figma 원본 가이드 페이지 자체의 번호(예: "05 Spacing", "06 Radius")를 따른다.

## 2. 폴더 구조

```
Style Guide/
├─ Style_Guide.md        ← 이 문서 (총괄 프로세스)
├─ Figma/                ← 단계별 "제작 규칙" md (크기·이름 등 원칙)
│   ├─ 01_FileName.md
│   ├─ 02_Layout.md
│   ├─ 03_Color.md
│   ├─ 04_1Font.md
│   ├─ 04_2Font_Name.md
│   ├─ 05_Spacing.md
│   ├─ 06_Radius.md
│   ├─ 07_Icon.md
│   └─ (향후 하위 섹션 추가 시) 등
└─ Document/             ← 단계별 "적용/실행 절차" md (Type 분류, Figma 반영 방법, 적용 이력)
    ├─ 02_Layout_Type.md
    ├─ 04_Font_Type.md
    └─ (향후) Color_Type.md 등
```

- `Figma/` 폴더: 값(크기, 이름 규칙 등)을 어떻게 정하는지에 대한 **원칙** 문서.
- `Document/` 폴더: 그 원칙을 실제 Figma 파일에 **어떻게 반영하는지**(절차, 분류 기준, 적용 이력) 기록하는 문서.
- 새 단계(Color, Layout, Icon 등)를 시작할 때도 이 폴더 구조와 네이밍 패턴(`{단계}.md`, `{단계}_Name.md`, `{단계}_Type.md` 등 필요한 만큼)을 그대로 따른다.

## 3. 완료된 단계별 문서

**1단계 — FileName**

- `Figma/01_FileName.md` — 화면(File)/레이어 이름 규칙, 아이콘·이미지·버튼 네이밍

**2단계 — Layout**

- `Figma/02_Layout.md` — 그리드(기준 해상도·컨텐츠 영역·컬럼·거터) 원칙, Grid Style 등록 절차. **가이드→프레임 적용**과 **프레임→가이드 정의**(메인 프레임에서 실제 그리드를 뽑아 가이드를 새로 정의/교정) 양방향 절차를 모두 포함. Division Spacing(간격) 관련 내용은 5단계(Spacing)로 분리됨
- `Document/02_Layout_Type.md` — 실제 프레임 분석 후 가이드 문서·Grid Style에 반영한 절차와 적용 이력

**3단계 — Color**

- `Figma/03_Color.md` — 컬러 가이드 문서 구조·네이밍 규칙, 컬러 토큰(Variables) 등록·바인딩 절차. **가이드→프레임 적용**(가이드가 이미 있을 때)과 **프레임→가이드 정의**(메인 프레임에서 색을 뽑아 가이드를 새로 만들 때) 양방향 절차를 모두 포함

**4단계 — Font**

- `Figma/04_1Font.md` — 폰트 패밀리·크기·줄간격 보정 규칙, 로고 예외
- `Figma/04_2Font_Name.md` — Text Style 네이밍(`Device_Size_Weight`) 및 정렬 규칙
- `Document/04_Font_Type.md` — Type(Title/Sub) 분류 기준, 번호 부여, 스타일가이드 문서 반영 절차

**5단계 — Spacing**

- `Figma/05_Spacing.md` — Division Spacing(4/8의 배수) 원칙, `Spacing` Variables 등록·정렬·바인딩 절차, 가이드 문서(룰러 다이어그램) 반영 절차. 원래 `02_Layout.md`에 함께 있던 내용을 분리·통합함

**6단계 — Radius**

- `Figma/06_Radius.md` — 코너 반경 네이밍(`Radius/{값}`, Full 예외), 4/8 배수 검증, 기존 토큰과 충돌 시 처리, 마스터 우선 바인딩, 가이드 문서 스와치 구성 절차. **프레임 → 가이드 정의**(역방향)로 시작한 사례

**7단계 — Icon (진행 중)**

- `Figma/07_Icon.md` — Keyline 개념(그리드값=Variant 번호=폴더명 일치 규칙), "01_Size" 차트 구성(인스턴스+라벨+스펙 텍스트), 컨테이너 폭 재조정 규칙, "02_Line" 라인 규칙(Area 비율 3종, 사이즈별 Stroke 두께 3구간). 아직 이 두 섹션 외 하위 항목(예: 실제 아이콘 목록·네이밍 등)은 다루지 않음 — 진행 시점에 추가 학습·문서화 예정

## 4. 공통 규칙 — 템플릿이 없으면 먼저 알린다

스타일 가이드 문서(표/카드 등 반복되는 레이아웃)를 Figma에 만들 때, **재사용 가능한 템플릿 컴포넌트가 이미 그 파일에 있는지 먼저 확인한다.**

- 템플릿이 있으면: 그 컴포넌트를 기반으로 인스턴스를 만들어 진행한다 (각 단계별 `Document/*.md`의 "템플릿 있음" 절차 참고).
- **템플릿이 없으면: 임의로 새로 만들어 진행하지 않는다.** 먼저 사용자에게 "이 파일에 템플릿이 없다"고 알리고, 원본 프로젝트 파일의 템플릿을 대상 파일에 복사해서 붙여넣어 달라고 요청한 뒤 작업을 진행한다.
  - 사용자가 템플릿을 붙여넣어주면, 붙여넣어진 컴포넌트가 **다른 파일에서 온 read-only 노드**일 수 있다는 점을 감안한다. 이 파일 소유로 완전히 독립시키려면 인스턴스를 detach한 뒤 `createComponentFromNode`로 새 로컬 컴포넌트를 만들고, 표 밖(프레임 밖)에 라벨과 함께 둔다.
  - 템플릿을 받기 전에 임시로 직접 표를 만들어 보여줄 수는 있지만, 이는 최소 기능(정보 정확성)만 보장하며 브랜드 비주얼과 다를 수 있다는 점을 사용자에게 분명히 알린다.
- 이 규칙은 Font 단계뿐 아니라 앞으로 진행할 Layout·Color·Icon 등 모든 단계에 동일하게 적용한다.

## 5. 전체 스타일가이드 감사(현행화) 절차

"이 스타일가이드 검수해줘", "바인딩 다 되어있는지 확인해줘" 처럼 받은 요청은 대상이 (A) 스타일가이드 개요 페이지 자체인지, (B) 이미 만들어진 디자인/프로덕션 프레임인지에 따라 아래 두 갈래로 나뉜다. 둘 다 "값은 Variables와 일치하는데 바인딩만 빠진 것"과 "값 자체가 Variables·규칙과 다른 것"을 구분해서 처리한다는 공통 원칙을 따른다.

### 5-A. 스타일가이드 개요 페이지 감사

**개별 단계가 아니라 전체 개요 페이지**(예: 01 FileName~06 Radius가 한 화면에 모여있는 노드)를 대상으로 받은 요청은 아래 순서로 진행한다.

1. 대상 노드의 직계 자식들을 나열해 각 하위 가이드 프레임(01 FileName, 02 Layout, 03-1/03-2 Color, 04 Font, 05 Spacing, 06 Radius 등)의 node-id를 확보한다.
2. 각 프레임을 **해당 단계의 `Figma/*.md`에 이미 정의된 "현행화" 절차**로 점검한다 — 새로 규칙을 만들지 않고 기존 문서를 그대로 따른다.
   - Color: `03_Color.md` 11-1번
   - Spacing: `05_Spacing.md` 6-4번
   - Radius: `06_Radius.md` 6-2번
   - Font: `Document/04_Font_Type.md` 5-C번(값 일치 검증)에 준해 Typeface 셀 바인딩 확인
   - Layout/FileName: 토큰 바인딩 대상이 아닌 문서(그리드는 그림으로만 표현, FileName은 네이밍 규칙 설명)이므로 대상에서 제외 — 그리드는 실제 적용 프레임(`Document/02_Layout_Type.md`) 쪽에서만 검증
3. 값 자체가 Variables와 다른 경우와, 값은 같은데 바인딩만 빠진 경우를 구분해 표로 정리한다.
4. **이 작업을 요청받았을 때 사용자가 "오류가 있으면 먼저 보고 후 확인받고 수정"을 명시하면, 개별 문서의 "값만 같으면 확인 없이 바로 바인딩" 예외 규정보다 사용자의 이번 지시가 우선한다** — 모든 발견 사항을 표로 보고하고 승인받은 뒤에만 수정한다.
5. 각 섹션의 공유 헤더(예: `Guideline_Top` 라이브러리 컴포넌트, "Design Guidelines" 타이틀바)가 외부/팀 라이브러리 스타일·변수를 참조하는 것은 정상이다 — 로컬 컬렉션에 없다고 오류로 보고하지 않는다.
6. 수정 후에는 해당 단계의 `Figma/*.md` 적용 이력에 감사 결과를 기록한다(어떤 항목을 확인했고, 무엇을 고쳤는지).

### 5-B. 실제 디자인/프로덕션 프레임 감사

이미 만들어진 화면(예: "Section 1" = Main/Sub/Sub 같은 실제 디자인 프레임)이 `Foundations` 등 Variables 기준을 제대로 반영하고 있는지 점검할 때는 아래 절차를 따른다.

1. **분석 → 탐색 → 검증 → 보고, 4단계 사이클로 진행한다.** 한 번의 스캔으로 끝내지 않는다.
   - **분석**: 대상 프레임의 모든 노드를 스캔해 fill/stroke/spacing(itemSpacing·padding)/radius 값을 (a) 이미 바인딩됨, (b) 값은 Variables와 일치하지만 미바인딩, (c) 값 자체가 불일치(미등록·4·8 배수 위반 등)로 분류한다.
   - **탐색**: (b)·(c) 각 항목이 인스턴스인지 마스터인지(`getMainComponentAsync()`), `COMPONENT_SET`이면 형제 variant도 같은 값인지, 인스턴스에 로컬 오버라이드(`instance.overrides`)가 걸려 마스터 수정만으로는 전파되지 않는지 확인한다.
     - **값을 실제로 고쳐야 하는 대상이 컴포넌트 인스턴스 내부 요소라면, 그 인스턴스가 아니라 부모(마스터) 컴포넌트를 찾아 마스터 쪽에 수정을 반영한다.** 이는 Variable 바인딩뿐 아니라 padding/itemSpacing 등 raw 값을 직접 고칠 때도 동일하게 적용한다(0번 마스터 우선 규칙의 일반화). 마스터 수정 전 `instance.overrides`로 해당 필드가 로컬 오버라이드되어 있지 않은지 먼저 확인하고, 수정 후에는 인스턴스들이 자동으로 새 값을 상속했는지 재조회로 검증한다.
   - **검증**: 변경 전/후 값을 재조회하고 `get_screenshot`으로 의도한 변경 외에 시각적 차이가 없는지 확인한다.
   - **보고**: 바로 처리한 항목과 보류한 항목을 나눠 최종 결과를 보고한다.
2. **(b) 값은 일치하는데 미바인딩인 항목은 확인 없이 바로 바인딩한다** — 시각적 변화가 없는 안전한 작업이다.
3. **(c) 값 자체가 다른(미등록·위반) 항목은 자동으로 고치거나 근사값으로 바인딩하지 않는다.** 먼저 목록으로 보고하고 사용자 판단을 기다린다.
   - **배너(banner)로 인식되는 요소에 적용된 컬러값은 특히 보수적으로 다룬다** — 자동 교체하지 않고 반드시 목록으로만 먼저 알린다(프로모션 등 의도적 예외색인 경우가 많음).
   - **간격(spacing) 위반 값은 목록으로 먼저 알린 뒤, 근사값으로 바꿀지 그대로 둘지 사용자가 판단하게 한다.**
   - **목록은 반드시 프레임 경로(레이어명 체인)로 표기한다.** 노드 id만으로는 위치를 파악하기 어렵다. 예: `Main > ++Top > + Banner > - Main banner > -- Search > -- Search bar > -- Icon (776:4855)`.
4. 사용자가 근사값 변경을 승인하면, 마스터 우선 규칙(0번)에 따라 인스턴스가 아닌 마스터(또는 해당 `COMPONENT_SET` variant)에서 값을 바꾸고 등록된 Variable에 바인딩한다. 값이 등록된 스와치 목록에 없으면(예: `0`) 바인딩 없이 raw 값만 수정한다 — `0`은 "간격 없음"이라 토큰이 아니다.
5. 수정 후 해당 프레임 관련 `Figma/*.md` 또는 이 문서 6번 적용 이력에 감사 결과를 기록한다.
6. **인스턴스에 `resetOverrides()`를 사용했던 영역을 재검수할 때는 스타일(색상·간격·코너 반경) 값뿐 아니라 컴포넌트 변형(variant)·`visible`·텍스트 콘텐츠까지 함께 확인한다.** `instance.overrides` 진단 배열은 완전하지 않아 이런 필드의 오버라이드를 누락 보고할 수 있고, 그 상태에서 `resetOverrides()`를 쓰면 값·바인딩은 정상으로 보여도 실제 콘텐츠가 조용히 유실될 수 있다 — 심한 경우 복구 불가능(버전 기록 접근 불가). 상세 원인·기법·사고 사례는 `05_Spacing.md` 6-3/6-3-1번 참고. 재검수 시 가능하면 **변경 전 스크린샷과 직접 대조**해 값뿐 아니라 화면에 보이는 내용 자체가 같은지 확인한다.
7. **`findAll()`로 인스턴스를 전수 스캔할 때, 특히 중첩된 오버라이드 경로 노드는 호출마다 결과 개수가 달라질 수 있다**(지연 로딩으로 추정). 한 번의 스캔 결과를 "전체"로 신뢰하지 말고, 같은 스캔을 2~3회 반복해 결과가 안정되는지 확인한 뒤 최종 목록으로 취급한다.

## 6. 적용 이력

| 대상 | 단계 | 내용 |
|---|---|---|
| 프로젝트 파일 | Layout | 02_Layout.md/02_Layout_Type.md 규칙 수립. "Main"(361:2446) 프레임 실측 기반으로 "02 Layout" 가이드(610:5082)의 그리드 수치·라벨 정정, `PC_12C` Grid Style 등록·연결 |
| 프로젝트 파일 | Color | 03_Color.md 규칙 수립. "03 Color" 가이드 문서 기준 컬러 토큰(Variables) 등록, "Main" 프레임에 정확 일치·근사값 바인딩 (가이드→프레임 적용) |
| 테스트 파일 | Color | 가이드→프레임 적용 방식 테스트 후, 반대로 메인 프레임에서 실제 색을 추출해 가이드를 새로 정의하는 절차(9번, 역방향) 최초 수립·적용 |
| 프로젝트 파일 | Font | 04_1Font.md/04_2Font_Name.md/Font_Type.md 규칙 수립, "03 Font" 스타일가이드 문서 제작 |
| 테스트 파일 | Font | 동일 규칙 테스트 적용. 템플릿 부재 상황 실제 발생 → 템플릿 요청 후 5-A 절차로 재제작, 이후 로컬 컴포넌트로 독립 |
| 프로젝트 파일 | Spacing | 05_Spacing.md 규칙 수립(02_Layout.md에서 분리·통합). `Spacing` Variables 등록 10개(4~120) → 세 프레임(361:2446/451:2289/514:6064) 728건 바인딩 과정에서 7개 추가(12·20·28·64·72·100·360) → 오름차순 정렬(전량 재생성) → node-id 776:4798("Section 1", 위 세 프레임과는 별개의 사본) 재조사로 `Spacing/360`(바깥 마진) 삭제, 최종 16개(4~120, 360 제외) 확정. "05 Spacing" 가이드(776:4673) 룰러 다이어그램 10→16개 사각형 재구성 |
| 프로젝트 파일 | Radius | 06_Radius.md 규칙 수립. Section 1(Main/Sub/Sub) 실측 기반 4/8/16/24/Full 확정(4/8 배수 위반 2건 사용자 확인 후 처리). 기존 세맨틱 네이밍 Radius 변수(파일 전체 4,000건 이상 바인딩)를 사용자 지시로 전량 삭제 후 `Radius/{값}` 네이밍으로 재등록, 마스터 우선 규칙으로 48개 대상에 바인딩. "06 Radius" 가이드 문서에 스와치 5개 구성 |
| "★ Style Guide" 페이지 / node-id 861:4230 ("Section 1", 01 FileName~06 Radius 개요 전체) | 5-A번 절차(스타일가이드 개요 감사) 최초 적용. 7개 섹션 전수 점검 — 03-2 Color(Semantic) 23개, 05 Spacing 16개, 06 Radius(Foundations 5+Semantic 2) 7개, 04 Font Typeface 16개는 전부 정상 바인딩 확인. **03-1 Color(Foundations)만 오류**: 팔레트 스와치 67개 전부 값은 일치하나 미바인딩 — 사용자에게 보고 후 승인받아 `setBoundVariableForPaint`로 67개 전량 바인딩(매칭 실패 0건, 시각적 변화 없음 확인). 02 Layout·01 FileName은 토큰 바인딩 대상 아님을 확인해 감사 범위에서 제외. 상세는 `03_Color.md` 적용 이력 참고 |
| 프로젝트 파일 / node-id 776:4798("Section 1": Main/Sub/Sub, 실제 디자인 프레임) | 5-B번 절차(실제 디자인 프레임 감사) 최초 적용, 분석→탐색→검증→보고 4단계로 진행. `Foundations` 기준 전수 스캔 → 값 일치·미바인딩 상태였던 색상 4건(White 3, Neutral/1A1A1A 1)·간격 318건(4~40px)을 확인 없이 바로 바인딩. 값 불일치 5건은 프레임 경로로 목록 보고: 배너 컬러 2건(`#DAE9FF` - Main banner, `#EEF7FF` - Feedback, 교체 보류), 간격 위반 3종(`1px`×6 - btn_더보기, `11px`×2 - Search Icon, `22px`×2 - Banner itemSpacing). 사용자 승인 후 `11→12`(Spacing/12)·`22→24`(Spacing/24) 바인딩, `1→0`(토큰 아님, raw 값만 수정) 확정 — 전부 마스터(`Button/TXT` 378:4040, `Banner`/`Type=Promo` 700:11813)에서 수정해 `instance.overrides`에 해당 필드가 없음을 확인 후 인스턴스에 자동 전파, 스크린샷 검증 완료. 배너 컬러 2건은 보류 유지 |
| 프로젝트 파일 / node-id 776:4798("Section 1") — Component(Card/Tag) 적용 재검수 | Card/Tag Component 시맨틱 토큰 적용(`06_Radius.md` 8번 참고) 직후 사용자 요청으로 5-B번 절차 재적용. GNB 검색 버튼 마스터(`484:4884`) `paddingRight`/`paddingTop` 11px 발견 → `Spacing/12`로 보정. 배너 컬러 1건(`Main banner`, `#DAE9FF`, `Foundations/Color/Sky/20`과 근사하나 불일치)은 목록 보고 후 보류 유지. 아이콘 내부 일러스트 벡터 색상은 토큰 대상에서 제외로 판단. `itemSpacing` 수백~천 단위 값 3건은 `SPACE_BETWEEN` 자동 계산값으로 확인해 위반 아님 처리 |
| 프로젝트 파일 / node-id 776:4798("Section 1") — 인스턴스 콘텐츠(변형·가시성·텍스트) 재검수 | 사용자가 변경 전/후 스크린샷을 직접 대조해 제공 → `resetOverrides()` 적용 영역에서 컴포넌트 변형(variant)·`visible`·텍스트 콘텐츠 유실을 다수 확인·복구. 홈페이지 "자주 묻는 질문" 카드 3개(Image variant), "최근 묻는 질문" 카드 4개(순위 배지 State·4번째 카드 숨김), "보험 Q&A" 리스트 페이지 카드 6개(Image variant·New 배지 가시성)와 사이드바 5개(순위 배지)를 프레임 경로 단위로 복구. "답변 근거" 참고자료 카드 2개는 인용문 텍스트가 영구 손실되어 복구 불가로 확정, 사용자에게 원문 재입력 요청. 상세 원인·기법·표준 절차 반영은 `05_Spacing.md` 6-3-1번과 이 문서 5-B번 6·7항 참고 |
| "★ Style Guide" 페이지 / node-id 861:4986 ("07 Icon") "01_Size" | 7단계(Icon) 최초 착수. 기존 항목 1개("16" 폴더, 실제로는 `Size=Keyline_12`/12×12 인스턴스+"12x12" 라벨 연결)가 이름과 내용이 어긋난 상태를 발견 → 사용자 확인 결과 "폴더명이 잘못 입력된 것"으로 확정, "16"→"12"로 정정. 이어서 원본 "Icon" 참조 페이지의 Keyline Guide(607:2899, COMPONENT_SET 607:2914)를 기준으로 나머지 11개 그리드값(16/20/24/32/40/48/56/64/72/80/104)을 같은 숫자의 Variant로 신규 인스턴스 생성해 추가, 12개 항목 전부에 5줄 스펙 텍스트(Pixel grid/Circle/Square/Rectangle H/V) 추가. 컨테이너 폭 재조정("01_Size"·"contents" 1920→2320, "07 Icon" 프레임 2120→2520) 후 스크린샷으로 클리핑 없이 전부 렌더링됨을 확인. 상세는 `07_Icon.md` 참고 |
| "★ Style Guide" 페이지 / node-id 861:4230 ("Section 1" 개요 전체, 10개 하위 섹션 — 00_Cover/Title/01~07) | 사용자 요청("스타일가이드 모두 바인딩, 없는 변수는 등록 불필요, 예외는 선보고")으로 5-A번 절차 재적용. 직계 자식 10개 확인 결과 **"07 Icon"이 node-id 877:11796으로, 기존에 작업했던 861:4986과 다른 별도 사본**임을 발견(내용은 01_Size 12개·02_Line 동일하게 이미 존재) — 05_Spacing.md 6-4번 6항에서 이미 알려진 "같은 이름의 가이드 프레임이 여러 사본으로 존재" 패턴과 동일 사례. 이 사본(877:11796) 기준으로 전수 재점검한 결과 **예외 없이 전부 정상 바인딩 확인**: 03-1 Color Foundations 67/67, 03-2 Color Semantic 23/23, 05 Spacing 16/16, 06 Radius(Foundations 5+Semantic 2) 7/7 전부 4개 코너 필드까지, 04 Font Typeface 16/16 전부 유효한 textStyleId 확인, 07 Icon 01_Size 12개 항목 전부 폴더명=Variant 번호 일치 확인, 02_Line 비율 벡터 3개 전부 `Color/Font/Strong`에 바인딩 확인. 02/03/04_Stroke 데모의 strokeWeight(1.2/1.5/2px)는 대응하는 Figma Variable이 없어 사용자 지시대로 신규 등록 없이 raw 값 그대로 둠. 00_Cover·Title·01 File Name·02 Layout은 기존 관례대로 토큰 바인딩 대상에서 제외. 수정 발생 0건(이미 전부 바인딩된 상태) |
| "★ Style Guide" 페이지 / node-id 861:4230 ("Section 1" 개요 전체) — 외부 라이브러리 변수 정리 | 사용자가 "타이틀 등에 이상한 값이 연결돼 있다"고 지적 → 전체 페이지를 fill/stroke 단위로 전수 스캔(로컬 Color/Foundations/Component 컬렉션에 없는 바인딩만 필터링, `Guideline_Top` 공유 헤더 내부는 정상 외부 참조이므로 스캔 제외). **외부 팀 라이브러리 변수 3종, 총 42건**을 발견: `Grays/Grays01`(#2D2D2D, 각 섹션 제목·설명문 24건+07 Icon 02_Line 라벨·데모 아이콘 stroke 12건=36건), `Color/font/font01`(#2D2D2D 동일값, 04 Font 표 헤더 라벨 8개+구분선 1개=9건 — 위 36건과 별개 항목이었던 425:6404/6407~6415 포함), `Grays/Grays02`(#505050, 02 Layout 그리드 범례의 점 1건). 로컬 컬렉션에 없는 값이라 사용자 지시대로 전량 해제 후, 원값과 가장 가까운 우리 시맨틱 토큰으로 재연결: `Grays/Grays01`·`Color/font/font01`(#2D2D2D) → `Color/Font/Strong`(#1A1A1A, 근사값 중 최소 차이), `Grays/Grays02`(#505050) → `Color/Font/Secondary`(#444444, 근사값 중 최소 차이). 일반 노드 36건은 한 스크립트로, 인스턴스 오버라이드 노드(07 Icon Stroke 데모 아이콘 내부 벡터, id에 `;` 포함) 6건은 별도 스크립트로 분리 처리(`03_Color.md` 6번 원칙). 처리 후 전체 재스캔 결과 외부 변수 참조 0건 확인, 스크린샷 대조로 시각적 변화 미미(어두운 회색 계열 내 색조 차이, 육안상 거의 동일) 확인 |
| "★ Style Guide"/"★ Component" 페이지 — 외부 라이브러리 변수 2차 정리(누락분) | 사용자가 "피그마 페이지에 없는 것들이 연결돼있다, 다 찾아봐"라고 재지적 → 직전 스캔이 paint-level(`fills[i].boundVariables.color`)만 보고 861:4230 서브트리로 범위가 좁았음을 확인, node-level 스칼라 바인딩(spacing/radius/opacity/effects 등)까지 포함한 전체 스캐너로 "★ Style Guide" 페이지 전체(3개 Section)를 재검사. 861:4230은 이미 0건이었으나 이전엔 몰랐던 **861:4983("Style Guide 기본 템플릿") 섹션의 "07 Icon" 세 번째 사본**에서 `Grays/Grays01` 13건 추가 발견 → `Font/Strong` 재바인딩(일반 7건·인스턴스 오버라이드 6건 분리 처리). 마스터 원본인 "★ Component" 페이지 "Icon" 섹션(607:2875)도 함께 스캔해 `Grays/Grays01` 6건과, **로컬 "Color" 컬렉션에서 삭제된 고아 변수 2종**(`Primary Color/Navy 10`→값 완전 일치하는 `Color/Navy/10`, `Primary Color/Blue 90`→값 완전 일치하는 `Color/Blue/90`)을 발견·수정. 재스캔 결과 두 페이지 모두 외부/고아 변수 참조 0건 확인. 상세는 `03_Color.md` 적용 이력 참고. "★ Component"의 GNB/Button/Image/Form/Board 등 다른 섹션은 이번 Icon/Style Guide 작업 범위 밖이라 미점검(필요 시 별도 요청) |
